@page "/"
@rendermode InteractiveServer

<PageTitle>Home</PageTitle>

<div class="dashboard">
    <h1>Welcome to RefConnect</h1>
    <p>Referee management system for sports championships</p>

    <div class="dashboard-grid">
        <div class="dashboard-card">
            <h3>📊 Statistics</h3>
            <p>View your statistics and performance metrics</p>
        </div>

        <div class="dashboard-card">
            <h3>🏆 Championships</h3>
            <p>Manage championships and tournaments</p>
        </div>

        <div class="dashboard-card">
            <h3>⚽ Matches</h3>
            <p>Schedule and track matches</p>
        </div>

        <div class="dashboard-card">
            <h3>👥 Teams</h3>
            <p>Manage teams and rosters</p>
        </div>
    </div>

    <div class="recent-activity">
        <h2>Recent Activity</h2>
        <div class="activity-list">
            @foreach (var activity in recentActivities)
            {
                <div class="activity-item">
                    <span class="activity-icon">@activity.Icon</span>
                    <div class="activity-content">
                        <strong>@activity.Title</strong>
                        <p>@activity.Description</p>
                        <small>@activity.Time</small>
                    </div>
                </div>
            }
        </div>
    </div>
</div>

@code {
    private List<Activity> recentActivities = new()
    {
        new Activity { Icon = "⚽", Title = "Match Scheduled", Description = "Team A vs Team B - Tomorrow at 3 PM", Time = "2 hours ago" },
        new Activity { Icon = "🏆", Title = "Championship Created", Description = "Spring League 2025 has been created", Time = "5 hours ago" },
        new Activity { Icon = "👥", Title = "New Team Added", Description = "FC United joined the platform", Time = "1 day ago" },
        new Activity { Icon = "💬", Title = "New Comment", Description = "Comment on Match #123", Time = "2 days ago" }
    };

    public class Activity
    {
        public string Icon { get; set; } = string.Empty;
        public string Title { get; set; } = string.Empty;
        public string Description { get; set; } = string.Empty;
        public string Time { get; set; } = string.Empty;
    }
}

